# deps
node_modules
.pnp
.pnp.js

# next
.next
out
build
.vercel

# env (never commit)
.env
.env.local
.env.development.local
.env.production.local
.nextresume-orders.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.eslintcache
tsconfig.tsbuildinfo
next-env.d.ts
.env*

# clerk configuration (can include secrets)
/.clerk/
.gstack/

# eval baselines (model output drifts; corpus itself is committed)
eval/*.jsonl

# real resumes used for local diagnosis — these carry personal contact details,
# so keep them out of history. Test fixtures are synthesised instead.
/*.pdf
/*.docx
/*.tex
